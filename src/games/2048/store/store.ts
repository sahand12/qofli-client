import { createStore } from 'redux';
import { actions } from './actions';

// function turnReducer(state = 0, action) {
//   if (action.type === actions.END_TURN) {
//     return state + 1;
//   }
//   return state;
// }
export {};
